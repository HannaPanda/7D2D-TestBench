<Window x:Class="Testbench.Gui.VersionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Testbench.Gui"
        Title="{local:Tr gui.versions.title}" Height="640" Width="900"
        Background="#12141A" Foreground="#E4E7EC"
        TextOptions.TextFormattingMode="Display"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <local:WarnBrushConverter x:Key="WarnBrush" />
        <local:OkBrushConverter x:Key="OkBrush" />
        <local:NotEmptyToVisibilityConverter x:Key="NotEmpty" />

        <SolidColorBrush x:Key="Panel" Color="#1A1D25" />
        <SolidColorBrush x:Key="Line" Color="#2B303B" />
        <SolidColorBrush x:Key="Muted" Color="#8A93A6" />

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E4E7EC" />
        </Style>

        <Style x:Key="Heading" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Muted}" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#E4E7EC" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#0D0F14" />
            <Setter Property="Foreground" Value="#E4E7EC" />
            <Setter Property="BorderBrush" Value="{StaticResource Line}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="5,3" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#242938" />
            <Setter Property="Foreground" Value="#E4E7EC" />
            <Setter Property="BorderBrush" Value="{StaticResource Line}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,7" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Opacity" Value="0.82" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#1E5F94" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" TextWrapping="Wrap" Foreground="{StaticResource Muted}" FontSize="12"
                   Margin="0,0,0,12"
                   Text="{local:Tr gui.versions.intro}" />

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding Root}" Margin="0,0,8,0" />
            <Button Grid.Column="1" Content="{local:Tr gui.versions.searchHere}" Click="Scan_Click" Margin="0,0,8,0" />
            <Button Grid.Column="2" Content="{local:Tr gui.versions.pickButton}" Click="Pick_Click" />
        </Grid>

        <Border Grid.Row="2" Background="{StaticResource Panel}" CornerRadius="7" Padding="12"
                BorderBrush="{StaticResource Line}" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="{local:Tr gui.versions.found}" Style="{StaticResource Heading}" />

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Candidates}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#171A22" CornerRadius="6" Padding="10" Margin="0,0,0,6">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="80" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <CheckBox Grid.Column="0" IsChecked="{Binding Take}"
                                                  IsEnabled="{Binding CanTake}" Margin="0,0,10,0" />
                                        <TextBox Grid.Column="1" Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}"
                                                 IsEnabled="{Binding CanTake}" Margin="0,0,10,0" />
                                        <StackPanel Grid.Column="2">
                                            <TextBlock Text="{Binding Dir}" FontFamily="Cascadia Mono, Consolas"
                                                       FontSize="11.5" TextWrapping="Wrap" />
                                            <TextBlock Text="{Binding Info}" FontSize="11" TextWrapping="Wrap"
                                                       Margin="0,3,0,0"
                                                       Foreground="{Binding Warn, Converter={StaticResource WarnBrush}}" />
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <TextBlock Grid.Row="3" Text="{Binding Status}" Margin="2,10,0,0" FontSize="12"
                   TextWrapping="Wrap"
                   Visibility="{Binding Status, Converter={StaticResource NotEmpty}}" />

        <Border Grid.Row="4" Background="{StaticResource Panel}" CornerRadius="7" Padding="12" Margin="0,12,0,0"
                BorderBrush="{StaticResource Line}" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="{local:Tr gui.versions.registeredTitle}" Style="{StaticResource Heading}" />
                <ItemsControl ItemsSource="{Binding Registered}" MaxHeight="180">
                    <ItemsControl.Template>
                        <ControlTemplate TargetType="ItemsControl">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsPresenter />
                            </ScrollViewer>
                        </ControlTemplate>
                    </ItemsControl.Template>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,0,5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="70" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Id}" FontWeight="SemiBold"
                                           VerticalAlignment="Center"
                                           Foreground="{Binding Installed, Converter={StaticResource OkBrush}}" />
                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Dir}" FontFamily="Cascadia Mono, Consolas" FontSize="11.5" />
                                    <TextBlock Text="{Binding Info}" FontSize="11" Foreground="{StaticResource Muted}" />
                                </StackPanel>
                                <Button Grid.Column="2" Content="{local:Tr gui.versions.remove}" Click="Remove_Click" Tag="{Binding}"
                                        Padding="8,3" FontSize="11" VerticalAlignment="Center" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="{local:Tr gui.versions.removeHint}"
                           Foreground="{StaticResource Muted}" FontSize="11" Margin="0,6,0,0" />
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="{local:Tr gui.versions.apply}" Style="{StaticResource PrimaryButton}"
                    Click="Apply_Click" Margin="0,0,8,0" />
            <Button Content="{local:Tr gui.close}" Click="Close_Click" />
        </StackPanel>
    </Grid>
</Window>
